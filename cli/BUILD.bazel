load("@rules_rust//rust:defs.bzl", "rust_binary")

package(default_visibility = ["//visibility:public"])

rust_binary(
    name = "belalang",
    srcs = glob(["src/**/*.rs"]),
    deps = [
        "//bir:bir",
        "//birgen:birgen",
        "//crates/ast",
        "//crates/lexer",
        "@crates//:clap",
        "@crates//:anyhow",
    ],
)
