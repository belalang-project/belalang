# RUN: %not %belalang build --emit ast %s 2>&1 | %FileCheck %s

struct T {
  a: Int
}

t := T {
  a := 1,
}

# CHECK:      {{.*}}/tests/AST/struct_literal_invalid_body.bel:8:3: error: error parsing struct: expected field assignment
# CHECK-NEXT:   a := 1,
# CHECK-NEXT:   ^
