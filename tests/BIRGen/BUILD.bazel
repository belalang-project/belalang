load("//tests:lit.bzl", "lit_test")

BELALANG_TESTS = glob(["*.bel"])

lit_test(
    name = "lit_test",
    tests = BELALANG_TESTS,
    data = [
        "//bin/belalang",
    ],
    env = {
        "BELALANG": "$(rlocationpath //bin/belalang)",
    },
)
